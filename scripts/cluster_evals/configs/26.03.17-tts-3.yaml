policy:
  config: "pi0_fast_full_droid_finetune"
  checkpoint_path: "/mnt/home_lustre/zavidili/openpi/checkpoints/pi0_fast_full_droid_finetune/pi0_fast_droid_finetune_bsz_256_lr_5e-5_frac_0.1/9999"
  run_dir: "/mnt/home_lustre/zavidili/openpi-geo"
  # Any extra flags forwarded verbatim to serve_policy.py, e.g.:
  #   "--tts-num-samples 8"
  #   "--tta-num-samples 4 --tta-aggregation mean"
  extra_args: "--tts-num-samples 3"

eval:
  # Explicit list or range strings (e.g. "0-9", "0,2,5-7") or a mix.
  task_ids: [0, 1]
  perturbation_ids: [0,1,3,4]
  repeats: 25
  max_steps: 800
  # Leave null to auto-generate from task_ids, perturbation_ids, max_steps, repeats.
  experiment_name: null

slurm:
  partition: "l40s"
  gpus: 1
  mem: "120G"
  cpus_per_gpu: 32
  time: "00-08:00:00"

base_port: 8000
debug: false
record_video: false
